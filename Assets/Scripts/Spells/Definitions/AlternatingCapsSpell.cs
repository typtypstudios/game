using UnityEngine;

//[CreateAssetMenu(fileName = "AlternatingCapsSpell", menuName = "TypTyp/Spells/AlternatingCapsSpell")]
public class AlternatingCapsSpell : SpellDefinition
{
    // Tras crear el sistema de efectos,
    // quiza no aporta nada crear clases de hechizos concretas,
    // ya que el propio sistema de efectos es lo suficientemente flexible
    // como para crear efectos muy variados sin necesidad de crear nuevas clases.

    // Por ejemplo, el efecto de AlternatingCapsEffect ya se puede usar para crear un hechizo que aplique ese efecto,

    // Sin embargo, por ahora voy a dejar esta clase como ejemplo de hechizo concreto
}